
# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Bruno — never commit local environment overrides that contain real credentials
# production.bru is committed with empty values; fill credentials locally only

